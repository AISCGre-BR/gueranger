{
  "mcpServers": {
    "gueranger": {
      "type": "stdio",
      "command": "node",
      "args": ["--env-file=.env", "./packages/mcp/build/server.js"],
      "env": {
        "DIAMM_USERNAME": "${DIAMM_USERNAME}",
        "DIAMM_PASSWORD": "${DIAMM_PASSWORD}"
      }
    }
  }
}
